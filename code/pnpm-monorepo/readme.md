#### 命令

```shell
全部安装
pnpm install (-w: 把包安装在 root 下, -r：把包安装在所有 packages 中 )

单个package添加
 pnpm add axios --filter basic-utils

单个package删除
 pnpm remove axios --filter basic-utils

 项目内引用
  pnpm add javascript-validate-utils --filter basic-utils
  
```